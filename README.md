# skin-disease-classification
Multimodal deep learning system for skin lesion classification, fusing CNN-extracted image features (ResNet18/EfficientNet transfer learning) with patient metadata (age, sex, site) via a joint neural network. Built on HAM10000, benchmarked against a Random Forest/XGBoost baseline, evaluated on accuracy and recall, and deployed as a Streamlit demo.
